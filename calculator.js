'use strict'

let number1 = +prompt("n1:");
if (isNaN(number1)) {
    alert("We must write a number, not a string");
}

let number2 = +prompt("n2:");
if (isNaN(number2)) {
    alert("We must write a number, not a string");
}

let result = 0;

/*=========================================================================*/

let sign = prompt("/,+,-,* :")

function calc() {
    alert("Sergey");
}

calc();